{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && python train_on_railway.py"
  },
  "deploy": {
    "startCommand": "gunicorn --timeout 300 --bind 0.0.0.0:$PORT app:app"
  }
}